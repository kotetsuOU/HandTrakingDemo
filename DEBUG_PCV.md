# RealTimeOcclusion PCV デバッグ＆空間演算システム設計思想・関数仕様ドキュメント

本ドキュメントは、点群（Point Cloud）データのリアルタイム可視化、CPU/GPU によるスパース点群の超高速近傍検索、および空間情報のフィルタリングをサポートする「デバッグ＆空間演算システム (PointCloudViewer)」の設計思想、各モジュールの役割、関数構成、および空間ハッシュ Compute Shader アルゴリズムの詳細を網羅したテクニカルリファレンスです。

---

## 🔗 統合プロジェクトポータル

本システムは、プロジェクトのメインポータルである **[RealTimeOcclusion システム統合 Wiki (WIKI.md)](./WIKI.md)** の「デバッグ空間検索ノード」として位置づけられています。

---

## 1. デバッグ＆空間演算システム概要

本システムは、膨大でスパース（まばら）な三次元点群空間を高速に走査し、デバッグ目的でのビジュアル確認、点群内の密度フィルタリング、あるいは位置補正や最接近点の衝突探索（Ray 最接近点判定）などを効率化するデバッグ・空間演算基盤です。

```
[Unity シーンビュー / インスペクタ]
               │
               ▼
       [PCV_Controller] (キャリブレーション姿勢補正)
        /            \
       /              \
      ▼                ▼
[PCV_VoxelGrid]   [PCV_GpuVoxelGrid]
(CPU 空間ハッシュ)  (GPU 空間ハッシュ / RsVoxelGridBuilder.compute)
  └─ 26 近傍走査     ├─ CSClearHashTable
     (`O(1)` 近傍検索) ├─ CSBuildGrid (InterlockedCompareExchange)
                     ├─ CSScanOffsets (プレフィックスサム)
                     └─ CSBuildIndices (バケットソート完了)
```

### 提供価値
*   **アセンブリリロード時の完全リークフリー保証**: 
    エディタ拡張や再生の切り替え時に Unity メモリに残留しやすいアンマネージド C++ リソースや GPU バッファを、イベント駆動で漏れなく確実に解放します。
*   **CPU 26近傍探索による線形探索排除**: 
    単純な総当たり判定（`O(N)`）を排除し、空間をグリッド分割してボクセルバケットに分類。指定座標の周囲 26 近傍のボクセルバケットのみを走査することで、近傍検索を実質的 `O(1)` で実行します。
*   **GPU 完全完結のハッシュ＆バケットソート**: 
    CPU を一切介さず、数百万点規模の点群に対する空間ハッシュテーブル、衝突回避ハッシュチェーン、およびインデックスの平坦化（バケットソート）を GPU 上だけで高速ビルドします。

---

## 2. 主要モジュールの設計思想と関数仕様

### 1. `PCV_Controller`
*   **設計思想**: PCV デバッグシステム全体の司令塔です。インスペクターの状態遷移を監視し、設定ファイルの変更・処理の切り替えをハンドリングします。
*   **座標補正機能（ApplyTransformCorrection）**:
    Unity 上でのキャリブレーション時に、Viewer 自身の Transform 行列を、登録されている全オブジェクトに適用して座標系を同期・リセットする位置補正・アライメント機能を備えています。

### 2. `PCV_DataManager` & `PCV_SpatialSearch`
*   **設計思想**: CPU での点群保持および空間検索のキャッシュを管理します。
*   **徹底したリーク対策**:
    Unity エディタの再生終了やアセンブリリロードなどのライフサイクルイベント（`AssemblyReloadEvents`）をフックし、メモリに残留しやすい C++ 由来のバッファや `ComputeBuffer` を漏れなく確実に破棄（`Release()`）します。

---

## 3. CPU 空間ハッシュアルゴリズム (`PCV_VoxelGrid`)

### A. ボクセル構築とグリッドハッシュ化
3D 空間上のスパースな点群を高速に探索するため、ハッシュマップ（`Dictionary<Vector3Int, List<int>>`）を用いた空間ボクセル構造を CPU 上で構築します。

1.  **ボクセルインデックスの算出**:
    空間内の点 `p = (x, y, z)^T` に対し、ボクセルサイズ `d_size` で除算して整数化（切り捨て）し、ボクセル座標 `v = (x_v, y_v, z_v)^T` を算出します。
$$
x_v = \lfloor x / d_{\text{size}} \rfloor, \quad y_v = \lfloor y / d_{\text{size}} \rfloor, \quad z_v = \lfloor z / d_{\text{size}} \rfloor
$$
2.  **ハッシュ登録**:
    算出したボクセル座標 `v` をキーとし、該当する頂点のインデックス `i` をリストにアペンドしてハッシュバケットを構築します（`BuildCpuGrid`）。

### B. 26 近傍走査による近傍探索
指定した中心座標 `p_center` の周囲半径 `R` 内の近傍点を探索する際、全点走査を行うと `O(N)` の時間がかかります。
`PCV_VoxelGrid` は、中心座標が属するボクセル `v_center` およびその周囲に隣接する **26 近傍のボクセル**（`3 * 3 * 3 = 27` 個のバケット）のみを走査します。

```
  ┌───┬───┬───┐
  │ ↖ │ ↑ │ ↗ │
  ├───┼───┼───┤   ← 探索中心 v_center の周囲
  │ ← │ ⭕ │ → │     26近傍 (2D平面上で9近傍の3D拡張版)
  ├───┼───┼───┤     のみに走査を限定
  │ ↙ │ ↓ │ ↘ │
  └───┴───┴───┘
```

走査範囲を極限まで絞り込むことで、探索コストを `O(1)` まで削減し、デバッグ Ray キャストや最接近点探索を超高速化しています。

---

## 4. GPU 空間ハッシュビルドパイプライン (`PCV_GpuVoxelGrid`)

GPU 上でスレッドセーフに空間ハッシュおよびソート済みインデックス配列を構築する `RsVoxelGridBuilder.compute` の動作仕様です。

```
1. CSClearHashTable (テーブルバッファを -1 で初期化)
          │
          ▼
2. CSBuildGrid (頂点ごとにハッシュ値を算出し、InterlockedCompareExchange でハッシュチェーン挿入)
          │
          ▼
3. CSScanOffsets (各ボクセルの点数から、平坦化配列上の開始オフセット [プレフィックスサム] を算出)
          │
          ▼
4. CSBuildIndices (各頂点を再走査し、オフセット位置にインデックスを格納 [バケットソート完了])
```

### 1. `CSClearHashTable` / `CSClearVoxelData`
*   **動作**:
    ハッシュテーブル配列 `_VoxelHashTable` をすべて `-1`（空き状態）にクリアし、ボクセルデータバッファを初期化します。

### 2. `CSBuildGrid` (アトミックハッシュチェーン挿入)
各頂点スレッドにおいて、3D座標からハッシュキー `H` を算出します。複数頂点が同一のハッシュキーを持つ「ハッシュ衝突」に対処するため、スレッドセーフなアトミック関数 `InterlockedCompareExchange` を用いたリンクリスト（ハッシュチェーン）を構築します。

*   **動作仕様**:
    1.  頂点の 3D グリッド座標からハッシュ値を計算します：
$$
H = \text{Hash}(\mathbf{v})
$$
    2.  アトミック操作でハッシュテーブルスロット `_VoxelHashTable[H]` を獲得します：
        ```hlsl
        InterlockedCompareExchange(_VoxelHashTable[H], -1, vertexIndex, prevHead);
        ```
    3.  もし衝突（すでに `prevHead != -1`）が発生した場合、自身のインデックスをリストの新しい先頭とし、以前の先頭 `prevHead` をハッシュチェーンバッファ `_VoxelHashChains[vertexIndex]` に書き込むことで、GPU上でスレッドセーフな衝突対応リンクリストを構築します。
    4.  ボクセル内の頂点カウントをアトミック加算します。

### 3. `CSScanOffsets` (プレフィックスサム / 掃き出し)
*   **動作**:
    ソートされた平坦な配列を生成するため、各ボクセルの累積頂点数をスキャンし、各ボクセルが平坦化配列のどこから始まるかの「開始オフセット」を算出します（プレフィックスサム演算）。

### 4. `CSBuildIndices` (並列バケットソート)
*   **動作**:
    全頂点を再度並列走査します。
    1.  頂点が属するボクセルの開始オフセットを取得します。
    2.  アトミック加算 `InterlockedAdd` を用いて、ボクセルごとのオフセット位置を一意に進めながら、ソート済みインデックスバッファ `_SortedIndices` に自身の頂点インデックスを格納します。
    3.  これにより、CPUへのコピーやソート関数の呼び出しを行うことなく、GPUメモリ上だけで完全にソートされたインデックス配列（バケットソート）が構築されます。

---

## 5. 動作検証 (Verification Plan)

### A. 静的検証
*   `RsVoxelGridBuilder.compute` 内の定数やバッファサイズ（オフセット、ハッシュサイズ等）が、[PCV_GpuVoxelGrid.cs](file:///c:/Users/hongo/Documents/tsutsumi/RealTimeOcclusion/Assets/Scripts/Debug/PointCloudViewer/PCV_GpuVoxelGrid.cs) のパラメータおよび `ComputeBuffer` 確保サイズと完全一致していることを確認します。

### B. デバッグ検証
*   PCV デバッグビューアをアクティブにし、インスペクターから探索半径を変更した際に、Gizmo で示された 26 近傍のボクセルバケットが正常に更新され、線形探索に比べて CPU/GPU 負荷が劇的に低下していることをプロファイラ（Unity Profiler / GPU Profiler）で検証します。
*   アセンブリリロードを複数回実行し、Unity コンソールにメモリリーク警告（`ComputeBuffer was not released`）が出力されないことを確認します。
